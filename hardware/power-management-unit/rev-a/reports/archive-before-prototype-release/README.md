# Historical checks before Rev A-P1

These are the previous final-report bytes and process manifests, preserved
before the stricter high-current geometry/rules and normal exit-0 checks.
The recorded timeout 124 is a failed process, even when the report has no
findings. Relative paths and hashes describe that historical CAD snapshot;
they are not claims about the current board. Current authority is
`../native-checks-both.json`, `../erc-final.json` and `../drc-final.json`.

Engineering prototype only; no production or hardware qualification.
