# High-current development diagnostics

Intermediate native DRC snapshots retained to explain rule scoping and copper
refill corrections. These include real violations and incomplete return paths
that were corrected later. They are not release checks or successful-process
claims. The final current-board ERC/DRC and their normal exit codes are recorded
one directory above in `native-checks-both.json`.

Engineering prototype only; no production or hardware qualification.
